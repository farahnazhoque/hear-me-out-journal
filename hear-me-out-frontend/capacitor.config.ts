import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hear-me-out-frontend',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
